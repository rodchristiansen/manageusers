name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Version from tag
        id: ver
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build universal binary
        run: swift build -c release --product manageusers --arch arm64 --arch x86_64

      - name: Build unsigned package
        env:
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          set -euo pipefail
          root="$PWD/pkg/root"
          rm -rf "$root" artifacts && mkdir -p "$root/usr/local/bin" "$root/Library/Management/Scripts" artifacts
          cp .build/apple/Products/Release/manageusers "$root/usr/local/bin/manageusers"
          chmod 755 "$root/usr/local/bin/manageusers"
          ln -s /usr/local/bin/manageusers "$root/Library/Management/Scripts/ManageUsers.sh"
          pkgbuild --root "$root" --identifier com.github.rodchristiansen.manageusers --version "$VERSION" \
            --install-location / "artifacts/ManageUsers-component.pkg"
          productbuild --package "artifacts/ManageUsers-component.pkg" --identifier com.github.rodchristiansen.manageusers \
            --version "$VERSION" "artifacts/ManageUsers-${VERSION}.pkg"
          rm -f artifacts/ManageUsers-component.pkg
          shasum -a 256 "artifacts/ManageUsers-${VERSION}.pkg" > "artifacts/ManageUsers-${VERSION}.pkg.sha256"

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          name: ManageUsers ${{ steps.ver.outputs.VERSION }}
          body: Unsigned package built from this tag. Sign and notarize it with your own Developer ID before deployment.
          files: |
            artifacts/ManageUsers-${{ steps.ver.outputs.VERSION }}.pkg
            artifacts/ManageUsers-${{ steps.ver.outputs.VERSION }}.pkg.sha256
